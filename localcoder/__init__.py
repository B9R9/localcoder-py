"""localcoder — a minimal local coding agent for Ollama.

Explicit context, zero bloat: no automatic repo scanning, no RAG unless you
build an index yourself, no tool advertised unless its dependency is
actually there. See README.md.
"""

__version__ = "0.1.0"
